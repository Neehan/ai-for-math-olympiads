You have only about {{tokens_left}} eligible output tokens remaining. Tools are disabled for this one final response. Stop exploring and consolidate between 1 and {{max_strategies}} semantically distinct whole-proof strategies now. Merge cosmetic duplicates and combine complementary components of one proof. Every entry must independently address the entire problem, including all directions, cases, constructions, and bounds; return fewer entries rather than fragments or cosmetic filler. End with `## Strategy Set`, followed only by one `<strategy>...</strategy>` tag pair per strategy. Do not write the final proof.
