You have only about {{tokens_left}} eligible output tokens remaining. Your entire response must fit within that amount. Tools are disabled: do no further exploration and immediately consolidate between 1 and {{max_strategies}} semantically distinct whole-proof strategies. Merge cosmetic duplicates and combine complementary components of one proof. Every entry must independently address the entire problem, including all directions, cases, constructions, and bounds. Keep each strategy at most 500 words and, if necessary to fit the budget, return fewer entries rather than fragments or cosmetic filler. End with `## Strategy Set`, followed only by one `<strategy>...</strategy>` tag pair per strategy. Do not write the final proof.
