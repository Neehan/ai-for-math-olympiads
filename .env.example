# Copy to .env and fill in. .env is git-ignored — never commit real tokens.
#
# Get a long-lived Claude Code OAuth token (subscription login):
#   claude setup-token
# then paste it here:
CLAUDE_CODE_OAUTH_TOKEN=
